name: 'fhir_pipeline'
version: '1.0.0'
config-version: 2

profile: 'fhir_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fhir_pipeline:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart
